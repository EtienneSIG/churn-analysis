# Python dependencies for local development and CI validation
# All notebook code runs inside Microsoft Fabric (PySpark environment).
# These packages support local linting, testing, and notebook validation.

# Notebook validation and inspection
nbformat>=5.9.0

# JSON schema validation
jsonschema>=4.20.0

# Local data inspection and CSV handling (optional — Fabric uses PySpark)
pandas>=2.0.0

# Testing
pytest>=7.4.0

# Microsoft Foundry — Part 2 (notebook 04: create the agent by code)
azure-ai-projects>=2.2.0
azure-identity>=1.17.0
openai>=1.50.0
